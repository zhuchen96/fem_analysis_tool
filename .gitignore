# the big raw data - way too big for git, everyone keeps their own copy
# and points these folders at it (see README "Getting the data")
/wildtyp_sample2_with_growth/
/Time series_flipped/
/smoothed_boundary.npz

# everything below here is generated by running the scripts - no reason
# to commit rendered frames or videos, just re-run the script that made them
/angle_histograms_*/
/overlay_groups_by_midline/
/overlay_groups_by_midline_expanded_mask/
/overlay_groups_by_midline_growth/
/vector_arrows_*/
*.mp4

# python noise
__pycache__/
*.pyc
.DS_Store
